test:
  override:
    - set -o pipefail && xctool -workspace KiwiTester.xcworkspace -scheme 'Kiwi Tester' -sdk iphonesimulator clean test PROVISIONING_PROFILE= CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY= | tee $CIRCLE_ARTIFACTS/xcode_raw.log
